# Existing entries below are already comprehensive. New customizations added!

# JetBrains Rider IDE specific files
.idea/
*.iml
# Rider logs and caches (adjusted so not to ignore meaningful configs)
.idea/workspace.xml
.idea/libraries/
.idea/shelf/
.idea/vcs.xml
.idea/modules.xml
.idea/.idea.*
# Rider plugin settings
.idea/dictionaries/
.idea/misc.xml
.idea/tasks.xml
# Rider .NET solution files
*.sln.iml
# Rider-specific caches
.cache/
# Unreal Engine references (if applicable in Rider setups)
.VSDir/

# Node.js specific files already mentioned but updated for Angular
node_modules/
.e2e/
*.tsbuildinfo # TypeScript incremental build files
dist/
build/
coverage/
.angular/ # Angular CLI cache
.output/ # Nuxt and similar builds

# Environment files for Angular (best practice to keep sensitive data out of version control)
.env
.env.*.local
.env.local
.local.env # Alternate naming conventions

# npm/Yarn/PNPM package managers - lock files
npm-debug.log*
yarn-error.log
.pnpm-debug.log*

# Temporary files created within .NET Core
*.vs/
artifacts/
*.nuget.targets
*.nuget.props
project.lock.json
project.fragment.lock.json
.nuget/

# .NET global configuration
.vscode/
.vscode/extensions.json
.vscode/*cache/ # safely add excludes workspace once caching builds toggled apps!
Lastly tighter Angular Live Preview (SRC..)!
**ignore{-#Disconnect tightening --ENDFILE}
...